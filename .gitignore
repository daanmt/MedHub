# PDFs e materiais binários (não versionar)
*.pdf
*.PDF

# Corpus de flashcards EMED (IP do EMED, derivado dos PDFs — local only, RAG/cunhagem)
*.cards.json
artifacts/emed_harvest_report.json

# Banco de Dados (local only)
ipub.db
data/chroma/
ipub.db-journal
medhub_memory.db
medhub_memory.db-journal
ipub_backup_*.db

# Python
.env
__pycache__/
.pytest_cache/
*.pyc
*.pyo
*.pyd
.Python

# Claude Code — worktrees temporários
.claude/worktrees/

# Pastas de sistema
.DS_Store
desktop.ini
Thumbs.db

# Obsidian — workspace state é local (lastOpenFiles, layout, etc.)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/appearance.json

# MCP — config local com paths de máquina
.mcp.json

# Codex CLI — config local com paths de máquina (paridade com .claude/, não versionar)
.codex/

# Scratch — scripts one-shot e temporários de sessão
tmp/
# s162: `scratch/` nasceu como irmão de `tmp/` e escapou desta regra por sessões
# (a s161 deixou 178 KB de dump de fila lá). Mesma função, mesmo tratamento.
scratch/

# Artefatos — backups e runs LLM (binários e JSONs grandes)
artifacts/backups/
artifacts/llm_runs/
artifacts/audits/

# /graphify output — regeneravel a partir do source (tools/, resumos/, etc.)
graphify-out/

# ledger-of-self: estado DERIVADO (reconstruivel); o .jsonl de eventos e versionado
history/ledger_self_state.json
# sink de falha da consolidacao de memoria (AGENTE.md §8): log de RUNTIME, nao artefato.
# O painel de DIVIDA le o arquivo no disco -- versionar so propagaria ruido de maquina.
history/memory_errors.log
core/simulados/_s*_questoes.json
