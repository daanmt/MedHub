# Snapshot freshness — known divergences

The documents in this directory and in `.vibeflow/index.md` were generated by the `gsd-map-codebase` and `vibeflow:analyze` skills between **2026-03-28 and 2026-04-23**. They have NOT been regenerated since. The codebase has drifted significantly. Use them for historical context, but verify any specific claim against the current tree.

## Divergences known as of 2026-05-27

### Deleted modules
- `app/components/sidebar.py`, `app/components/__init__.py`
- `app/utils/parser.py`, `app/utils/file_io.py`
- `app/engine/analyze_error.py`, `app/engine/get_review_queue.py`, `app/engine/generate_flashcards.py`
- `app/memory/tools.py`
- `Tools/sync_flashcards.py`

### Deleted earlier (already absent in some snapshots)
- `app/pages/4_simulados.py` — the "Simulados RAG" page
- `app/utils/flashcard_builder.py` — was renamed to `app/engine/generate_flashcards.py`, then deleted
- `app/utils/db.py::sync_git()` — removed during cleanup
- `medhub-ui-refresh-main/` — abandoned React project, removed from tree (still in git history)
- `caderno_erros.md`, `caderno_erros.md.bak` — archived to `history/legacy/`
- `flashcards_cache.json` (root) — archived to `artifacts/legacy/`

### Moved
- `Temas/` → `resumos/` (clinical knowledge layer)
- 6 one-shot migration scripts → `Tools/_archive/migrations/` (`migrar_sessoes_bulk.py`, `migrate_flashcards.py`, `migrate_memory.py`, `popular_subtemas.py`, `fix_taxonomy_bridge.py`, `cleanup_db.py`)
- `HANDOFF.md` (root) — retired sessão 058; only `artifacts/legacy/HANDOFF_056.md` survives

### Added since these snapshots
- `LICENSE` (MIT) at repo root
- `Tools/eval/` — reproducible RAG retrieval eval (`queries.json` + `run_eval.py` + `README.md` + first `REPORT.md` baseline)
- `Tools/_archive/migrations/README.md`

### Schema / engine API
- `Tools/init_db.py` rewritten to produce the live v5 schema (`flashcards` v5 columns, `sessoes_bulk` table). The version in the snapshots produced the v1 schema which would not boot the live app.
- `app/engine/__init__.py` now exports **2** functions (`get_topic_context`, `summarize_performance`), not 5. Documentation that mentions `analyze_error` / `get_review_queue` / `generate_contextual_cards` as part of the engine surface is stale.
- `app/memory/__init__.py` no longer exports `get_memory_tools`.

### Tracking / hygiene
- `.obsidian/workspace.json` untracked + gitignored (leaked personal `lastOpenFiles`).
- `requirements.txt` now pins `anthropic`, `chromadb`, `langchain-anthropic`, `pydantic`.

## Refreshing these docs

Re-run `vibeflow:analyze` (for `.vibeflow/index.md`) or `gsd-map-codebase` (for `.planning/codebase/*`) to regenerate from the current tree. Until that's done, treat the dated `Analyzed:` / `Analysis Date:` line at the top of each file as authoritative — any claim newer than that line is not in the snapshot.

This NOTICE is the source of truth for divergences. The next snapshot regeneration should delete it.
