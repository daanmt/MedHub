# HANDOFF.md -- ESTADO OPERACIONAL CURTO
*Atualizado: 2026-08-18 -- Rescoping ENAMED (guia oficial) + aula-base CA Mama D10 + reestruturação da Autópsia (sessão 147)*

## > Próximo passo imediato

1. **Usuário está executando agora (fora desta sessão):** S15 -- Princípios e Diretrizes do SUS (Revisão) + Asma (Revisão) à frio, e Câncer de Mama (Teoria) com a aula-base D10 já entregue nesta sessão. Total 103 questões + 100 flashcards (10 blocos de 10). Ele **reinicia sessão nova hoje** para trazer os comentários dos erros dos 3 blocos + os dados de performance. **Enquanto aguarda ele terminar: corrigir o bug do highlight da Autópsia** (ver Pendências abaixo -- diagnóstico já adiantado, comece pela inspeção do HTML renderizado). Quando ele trouxer os resultados: rodar `/analisar-questao` (ou paralelizar em subagents como no Simulado 5) sobre os erros que saírem, registrar volume via `registrar_sessao_bulk.py` ANTES de processar erros, e drenar a fila de flashcards.
2. **Cronograma real (Drive) diverge do que o `grade.json` capta.** O usuário reorganizou `Cronograma de Reta Final.xlsx` por cores (Roxo > Rosa > Salmão = prioridade). S15 real: SUS(Rev), Asma(Rev), CA Mama(Teoria), Aleitamento Materno(Teoria), Parasitoses;Infecções Rel. à Assist. em Saúde(Teoria), Apendicite Aguda;Colecistite e Colangite Aguda;Diverticulite Aguda(Rev.Questões). S16 real: Atenção Primária(Teoria II), Aleitamento Materno(Rev), CA Mama(Rev), Assistência ao Parto(Rev), Parasitoses;Infecção Hospitalar(Rev), Doenças Exantemáticas;Imunizações;Asma;Aleitamento Materno(Rev.Questões), Planejamento Familiar;Vulvovaginites;Endometriose;CA Mama(Rev.Questões). **Achado técnico:** as tarefas "Revisão por Questões" que o `grade.json`/PDF trazem com `tema=""` TÊM tema real na planilha do Drive -- o parser do PDF perde essa informação. Não investigado a fundo ainda.
3. **Rescoping ENAMED concluído nesta sessão** (ver Estado por frente). Usuário disse "vou revisar e aplicar os ajustes à grade" -- aguardar se ele reporta divergências do de-para aplicado.

## Estado por frente
- **Rescoping ENAMED:** `core/simulados/guia_enamed.json` (Guia Estatístico ENAMED extraído do PDF público da Estratégia MED -- Revalida INEP 2011-2025 + prova real do ENAMED 2025/2026 -- substitui o guia ENARE antigo e o "blueprint medido" enviesado pela fraqueza do usuário). `core/cronograma/links_exercicios.json`: URL do caderno de exercícios por (semana,tarefa), S15-S30, extraído por posição do `Cronograma.pdf` e validado 16/16 semanas (soma de questões bate 100% contra o total oficial). `core/simulados/rescoping_final_com_links.json`: 92 blocos pendentes por peso de prevalência real, com Nova Semana (empacotamento a 500q/semana = 100q/dia x5d) e links. Artifact **"Fila ENAMED"** (`.../93162128-...`) publicado com a tabela completa + troca entram/saem + notas de cautela (ex.: HAS sai da janela por Cardiologia ser especialidade pequena, não por ser fraca).
- **Autópsia dos Simulados** (`.../c414a4f3-...`) reestruturada por feedback do usuário: blocos "discriminador que faltou"+"por que é armadilha"+"o que faltou" fundidos em "Anatomia do erro" (após a cadeia de habilidades, antes de "Racional correto"); dados-chave (números+unidade, negações/exceções) destacados na vinheta via `<mark>`; 3 legendas decorativas removidas; heurística de `elo_quebrado` recalibrada (viés posicional 0.12->0.38, mitiga falso-positivo de cobertura lexical em blocos curtos); dicionário de acentuação `ACC` em `tools/autopsia_simulados.py` expandido de ~200 para ~950 entradas (regra dura mantida: nenhum verbo conjugado ambíguo entra).
- **Resumo `resumos/GO/[GIN] CA de Mama.md`** corrigido/enriquecido contra o PDF-fonte do EMED (3 PDFs lidos): correção real (Ki-67 corte é 20%, não 14% como estava) + tabela de rastreio por critério de alto risco (idade amarrada ao critério, não genérica) + BI-RADS 4A/4B/4C + vocabulário de imagem + tipos de biópsia + Oncotype DX + discriminador de dor no carcinoma inflamatório + exceção PRIME-2 + exceção artrite reumatoide.
- **Volume & Metas:** 6.119 acumulado (sem volume novo registrado nesta sessão -- é frente de engenharia/aula, não questões). Ritmo-alvo ~49q/dia segue valendo (26d até ENAMED nesta data).
- **Datas:** ENAMED 13/09/2026 (26d) -- grade fecha 25/10/2026.
- **Nada commitado até o fechamento desta sessão** -- selado no mesmo commit deste HANDOFF.

## Última sessão -- s147 (RESCOPING ENAMED + AULA CA MAMA + AUTÓPSIA REESTRUTURADA)
Sessão de engenharia + conteúdo, sem volume de questões novo. Três frentes: (1) resolvida a tensão entre 3 fontes de prevalência (guia ENARE antigo/prova extinta, "medido" via simulados enviesado pela fraqueza do usuário, guia ENAMED novo achado via pesquisa) -- usuário corretamente apontou o viés do "medido"; guia ENAMED (INEP oficial) venceu como fonte única; rescoping completo produzido com links de exercícios reais extraídos por posição do PDF. (2) Aula-base D10 de Câncer de Mama ancorada nos 6 erros já catalogados do usuário (4 do tema, 2 de Doenças Benignas adjacente), fio condutor = padrão de inversão/generalização de fronteira de categoria (Ki-67, alto risco x geral, mamografia x radioterapia na gestação); resumo corrigido contra 3 PDFs-fonte. (3) Autópsia dos Simulados reestruturada por feedback point-a-point do usuário (fusão de blocos, highlight, remoção de legendas, acentuação, heurística de quebra).

## Pendências/observações ativas
- 🔴 **Bug: highlight de dados-chave na vinheta da Autópsia não aparece na prática** (reportado pelo usuário logo após o fechamento da s147). Diagnóstico já adiantado para não reinvestigar do zero: a regex `KEYRX`/`marcaChave()` funciona isolada (testado em Node: `"43 anos"`, `"4 dias"`, `"sem"` deram match e substituição corretos no texto real de uma vinheta) -- o bug não é na regex. O código-fonte também confere: `bloco("A vinheta", e.enun, "", true, true)` chama `hl=true`, e `bloco()` faz `hl ? marcaChave(esc(txt)) : esc(txt)`. Suspeitos a checar primeiro: (a) `<mark>` sendo inserido no DOM mas o CSS `.etext mark.dk{...}` perdendo para o estilo nativo do browser por especificidade/cascata -- inspecionar o HTML renderizado real (abrir `artifacts/autopsia-simulados.html` num browser, abrir um erro, inspecionar elemento na vinheta) para confirmar se a tag `<mark class="dk">` está lá; (b) alguma etapa de minificação/serialização entre a geração e a publicação via Artifact tool alterando o `<script>`. Comece por (a) -- é o teste mais rápido e decisivo.

- `AUDITORIA_MEDHUB.md` segue ledger vivo com achados abertos (F21/F35/F36-ALTA/F37/F38/F39) -- não tocado nesta sessão.
- `tools/fila_enamed.py` (script antigo, "A Fila Errada") está **superado** pelo rescoping desta sessão (`guia_enamed.json` + `rescoping_final_com_links.json`) -- não deletado, mas não é mais a fonte a consultar. Considerar aposentar formalmente numa sessão futura (checar alcançabilidade).
- Achado técnico não resolvido: `tools/cronograma.py`/parser do PDF perde o tema de tarefas "Revisão por Questões" que a planilha do Drive tem preenchido (ver Próximo passo #2).
- Guias estatísticos de UERJ/USP (fase 2, pós-ENAMED) ainda não existem no repo -- fica para depois do ENAMED, conforme o próprio usuário definiu.

---
*Histórico: history/INDEX.md * Macro: ESTADO.md * Sessão: history/session_147.md*
